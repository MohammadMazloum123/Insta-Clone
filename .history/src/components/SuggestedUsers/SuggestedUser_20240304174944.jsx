import { useState } from "react";
import { Avatar, Flex, Button, VStack,  } from "@chakra-ui/react";
const SuggestedUser = ({followers, name, avatar}) => {
    const [isFollowed, setIsFollowed] = useState(false)
    return (
        
    )
}

export default SuggestedUser;
